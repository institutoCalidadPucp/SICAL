(function() {
  $(function() {
    $('.permits-table').on('cocoon:before-insert', function (e, node) {
      // var container = node.first();
      // console.log(node.children().children());
    })
  })
})();