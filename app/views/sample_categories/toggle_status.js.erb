(function() {
  $(function() {
    var $currentLock = $('#sample-category-lock-<%= @sample_category.id %>');
    var status = "<%= @sample_category.status %>";

    toggleLockStatus($currentLock, status);
  });
})();
