<div id="employee-assign" class="tabcontent">
  <table class="table table-hover sical-data-table sical-data-table"  data-placeholder="Buscar Muestra">
    <thead>
      <th>Código de muestra</th>
      <th>Categoría</th>
      <th>Método</th>
      <th>Acreditado</th>
      <th>Asignación</th>
    </thead>
    <tbody>
    <% @service.sample_processeds.each.with_index(1) do |sample_processed, index| %>
        <tr>
          <td><%= sample_processed.description %></td>
          <td><%= sample_processed.sample_category.name %></td>
          <td><%= sample_processed.sample_method_name %></td>
          <td><%= sample_processed.sample_method_accredited? %></td>
          <td><%= f.collection_select :id, @employees,:id, :name,{}, {name: "selected_employee_#{index}"} %></td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>