<div class="title"><%= title %></div>
<hr>
<div class='mailbox-messages flex-1'>
  <div class="scroll">
    <table>
      <% if quotations.empty?%>
        <div class="mailbox-unread-messages">
          You have no unread messages.
        </div>
      <% else %>
        <% quotations.each do |quotation| %>
          <tr class='message-row' >
            <td class="time">
              <%= quotation.created_at.strftime('%I:%M %p')[0...-1] %><br>
              <%= quotation.created_at.strftime('%^b %d') %>
            </td>
            <td>
              <%= link_to edit_service_path(quotation) do %>
                <%= quotation.subject %><br>
              <% end %>
            </td>
          </tr>
        <% end %>
      <% end %>
    </table>
    <%#= link_to , id: 'more-messages', class: 'flex-container' do %>
      <span class='mailbox'></span><span class='text flex-1'>View All Messages</span><span class='next'></span>
    <%# end %>
  </div>
</div>

<script type="text/javascript">
//$(document).ready(scroll('y'));
</script>