<div class="material-datatables">
  <table class="table table-hover sical-data-table sical-data-table" data-placeholder="Buscar Item">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Marca</th>
        <th>Modelo</th>
        <th>Estado</th>
        <th>Acciones</th>
      </tr>
    </thead>
    <tbody>
        <% inventories.each do |inventory| %>
            <tr>
              <td><%= inventory.name %></td>
              <td><%= inventory.brand %></td>
              <td><%= inventory.model %></td>
              <td><%= inventory.status %></td>
              <td>
                <%= link_to '<i class="fa fa-eye" aria-hidden="true"></i>'.html_safe, show_inventory_path(inventory), class: "btn btn-info btn-sm", data: { "toggle" => "tooltip" }, title: "Ver" %>
                <%= link_to '<i class="fa fa-pencil" aria-hidden="true"></i>'.html_safe, edit_inventory_path(inventory), class: "btn btn-success btn-sm", data: { "toggle" => "tooltip" }, title: "Editar" %>
                <%= link_to inventory_path(inventory),class: "btn btn-default btn-sm", data: { "toggle" => "tooltip" }, title: "Activar/Desactivar", method: :delete do%>
                  <% if inventory.status == "active" %>
                    <i class="fa fa-lock" aria-hidden="true"></i>
                  <% else %>
                    <i class="fa fa-unlock" aria-hidden="true"></i>
                  <% end %>
                <% end -%>
              </td>
            </tr>
        <% end %>
    </tbody>
  </table>
</div>